import React, { FC } from "react";
import WeatherInfo from "./WeatherInfo";

type Props = {};

const WeatherInfoContainer: FC<Props> = () => {
  return <WeatherInfo />;
};

export default WeatherInfoContainer;
