import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 16,
  },
  subtitle: {
    fontSize: 14,
    marginTop: 16,
    marginBottom: 16,
  },
  valueText: {
    fontSize: 24,
    fontWeight: "bold",
  },
});